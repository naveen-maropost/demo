.container
  .col-md-3.margin20
    .card{:style => "width: 20rem;"}
      %img.card-img-top{:alt => "Card image cap", :src => "#{@gallery.attachment.url(:large)}"}/
      .card-block
        %h4.card-title
          = link_to @gallery.name, edit_gallery_path(@gallery, :format => :js), remote: true, title: "Click to Edit Name"
        = link_to "Back", :back

.popup_data